package fse.team2.common.websockets.commands.enums;

/**
 * Represents possible commands the server can send to the client.
 */
public enum ClientCommandType {
    RECEIVE_MESSAGE,
    SEND_MESSAGE_SUCCESS
}
